{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "find . -name 'pom.xml' && ls -la && mvn clean install -DskipTests || (cd user-service-app && mvn clean package -DskipTests)"
  },
  "deploy": {
    "startCommand": "find . -name '*.jar' -type f ! -path '*/original/*' | head -1 | xargs java -jar",
    "restartPolicyType": "ON_FAILURE"
  }
}